#include<iostream>
#include<stack>
#include<vector>
using namespace std;
vector<int> findans(vector<int> arr){
    int n=arr.size();
    stack<int> st;
    st.push(arr[0]);
    for(int i=n-2;i>=0;i--){

    }
}
int main(){
    vector<int> arr={5,15,10,8,6,12,18};
    vector<int> vec=findans(arr);
}